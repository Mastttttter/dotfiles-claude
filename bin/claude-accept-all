#!/usr/bin/env bash

set -euo pipefail

config="${CLAUDE_CONFIG_DIR:-$HOME}/.claude.json"
tmp="$config.tmp.$$"

jq '.projects |= with_entries(.value.hasTrustDialogAccepted = true)' "$config" >"$tmp"
mv "$tmp" "$config"

jq -r '.projects | length as $n | "trusted \($n) projects"' "$config"
